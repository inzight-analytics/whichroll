---
import { navItems } from '../config/nav';

interface Props {
  variant?: 'overlay' | 'solid';
}

const { variant = 'solid' } = Astro.props;
const currentPath = Astro.url.pathname;
const isOverlay = variant === 'overlay';
---

<header
  class:list={[
    'relative z-50 w-full',
    isOverlay ? 'absolute inset-x-0 top-0 bg-transparent' : 'bg-roll-orange',
  ]}
>
  <div class="mx-auto flex max-w-[1410px] items-center justify-between gap-4 px-4 py-4 sm:px-6 lg:px-8">
    <a href="/" class="shrink-0" aria-label="Which Roll? home">
      <img
        src="/images/Which-Roll-02.png"
        alt="Which Roll? – Ko Tēhea Rārangi Pōti?"
        class="h-[110px] w-auto"
        width="280"
        height="119"
      />
    </a>

    <button
      type="button"
      id="nav-toggle"
      class="inline-flex flex-col items-center justify-center gap-1.5 rounded-md p-2.5 lg:hidden [-webkit-tap-highlight-color:transparent]"
      aria-label="Toggle menu"
      aria-expanded="false"
      aria-controls="site-nav-mobile"
    >
      <span class="block h-0.5 w-6 bg-white"></span>
      <span class="block h-0.5 w-6 bg-white"></span>
      <span class="block h-0.5 w-6 bg-white"></span>
    </button>

    <nav
      id="site-nav"
      class="hidden lg:block"
      aria-label="Main navigation"
    >
      <ul class="flex items-center gap-1 xl:gap-2">
        {
          navItems.map(({ href, label }) => (
            <li>
              <a
                href={href}
                class:list={['nav-link', currentPath === href && 'nav-link--active']}
                aria-current={currentPath === href ? 'page' : undefined}
              >
                {label}
              </a>
            </li>
          ))
        }
      </ul>
    </nav>
  </div>

  <!-- Mobile menu — absolute overlay, no layout shift -->
  <nav
    id="site-nav-mobile"
    class="absolute inset-x-0 top-full z-50 hidden border-t border-white/20 bg-roll-orange shadow-lg lg:hidden"
    aria-label="Mobile navigation"
  >
    <ul class="flex flex-col px-4 py-3">
      {
        navItems.map(({ href, label }) => (
          <li>
            <a
              href={href}
              class:list={[
                'nav-link py-3 text-base xl:px-3 xl:py-2 xl:text-sm',
                currentPath === href && 'nav-link--active',
              ]}
              aria-current={currentPath === href ? 'page' : undefined}
            >
              {label}
            </a>
          </li>
        ))
      }
    </ul>
  </nav>
</header>

<script>
  const toggle = document.getElementById('nav-toggle');
  const mobileNav = document.getElementById('site-nav-mobile');

  toggle?.addEventListener('click', () => {
    const isOpen = mobileNav?.classList.toggle('hidden') === false;
    toggle.setAttribute('aria-expanded', String(isOpen));
  });
</script>
